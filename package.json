{
    "name": "lesli-nodejs-mongodb-tools",
    "version": "0.4.2",
    "description": "MongoDB tools for Node.js applications",
    "homepage": "https://lesli.tech",
    "author": "The Lesli Development Team <hello@lesli.tech>",
    "license": "GPL-3.0",
    "main": "index.js",
    "dependencies": {
        "mongodb": "3.6.6"
    },
    "devDependencies": {
        "chai": "4.3.4",
        "mocha": "8.3.2"
    },
    "scripts": {
        "test": "clear && mocha --reporter spec 'tests/**/*.test.js'",
        "test:utils": "clear && mocha --reporter spec 'tests/utils/*.test.js' --exit",
        "test:query:database": "clear && mocha --reporter spec 'tests/query/database.test.js' --exit",
        "test:query:collection": "clear && mocha --reporter spec 'tests/query/collection.test.js' --exit",
        "test:query:document": "clear && mocha --reporter spec 'tests/query/document.test.js' --exit"
    }
}
